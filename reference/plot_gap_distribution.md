# Distribution of per-provider maximum gaps

Histogram of each provider's longest inter-exposure gap, with the
readiness threshold marked.

## Usage

``` r
plot_gap_distribution(sim, bins = 30)
```

## Arguments

- sim:

  A run `halo_sim` object.

- bins:

  Integer histogram bins.

## Value

A ggplot object.
