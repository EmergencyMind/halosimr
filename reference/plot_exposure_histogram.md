# Distribution of per-provider exposure counts

Distribution of per-provider exposure counts

## Usage

``` r
plot_exposure_histogram(sim, bins = 30)
```

## Arguments

- sim:

  A run `halo_sim` object.

- bins:

  Integer histogram bins.

## Value

A ggplot object.
