% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{halo_sim}
\alias{halo_sim}
\title{Construct a HALO simulation}
\usage{
halo_sim(
  schedule,
  events,
  providers = NULL,
  seed = 42,
  readiness_model = "binary",
  readiness_threshold_days = 90,
  readiness_half_life_days = 60,
  ebbinghaus_b = 0.05,
  step_partial_value = 0.5,
  step_t2_days = 180,
  training_program = "none",
  training_interval_days = 28,
  training_start_day = 0
)
}
\arguments{
\item{schedule}{Character (providers x days) matrix of \code{"d"}/\code{"n"}/\code{"o"},
e.g. from \code{\link[=generate_schedule]{generate_schedule()}}.}

\item{events}{Data frame of events with \code{day_idx} and \code{shift_type}, e.g. from
\code{\link[=generate_events]{generate_events()}}.}

\item{providers}{Optional character vector of provider ids (defaults to
\code{P0001}, \code{P0002}, ...).}

\item{seed}{Integer seed for the warmup draw.}

\item{readiness_model}{One of \code{"binary"}, \code{"exponential"}, \code{"ebbinghaus"},
\code{"step"}.}

\item{readiness_threshold_days}{Integer threshold for \code{binary}/\code{step} and the
gap exceed flag.}

\item{readiness_half_life_days}{Numeric half-life for \code{exponential}.}

\item{ebbinghaus_b}{Numeric shape for \code{ebbinghaus}.}

\item{step_partial_value, step_t2_days}{Partial value and second threshold for
\code{step}.}

\item{training_program}{One of \code{"none"}, \code{"monthly"}, \code{"bimonthly"},
\code{"quarterly"}, \code{"custom"}.}

\item{training_interval_days}{Integer interval for \code{custom}.}

\item{training_start_day}{Integer 0-based day of first training.}
}
\value{
An object of class \code{halo_sim}.
}
\description{
Construct a HALO simulation
}
\examples{
sched <- generate_schedule(20, 90, "3/7 Day", seed = 1)
ev <- generate_events(90, rate = 0.2, seed = 1)
sim <- halo_sim(sched, ev, readiness_model = "exponential")
sim <- hsrun(sim)
}
