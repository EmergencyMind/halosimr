% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{generate_events}
\alias{generate_events}
\title{Generate a Poisson HALO event stream}
\usage{
generate_events(
  n_days,
  rate,
  seed = 42,
  day_rate = NULL,
  night_rate = NULL,
  start_date = "2024-01-01"
)
}
\arguments{
\item{n_days}{Integer window length.}

\item{rate}{Numeric events per day, split 50/50 day/night. Ignored when both
\code{day_rate} and \code{night_rate} are supplied.}

\item{seed}{Integer RNG seed.}

\item{day_rate, night_rate}{Optional numeric per-day rates for each shift.
Supply both to override \code{rate}.}

\item{start_date}{Character or Date; day 0 of the window.}
}
\value{
A data frame with columns \code{day_idx} (integer, 0-based), \code{date}
(Date), and \code{shift_type} (\code{"day"}/\code{"night"}), sorted by day.
}
\description{
Generate a Poisson HALO event stream
}
\examples{
ev <- generate_events(365, rate = 0.1, seed = 1)
nrow(ev)
}
