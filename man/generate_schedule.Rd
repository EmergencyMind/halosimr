% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schedules.R
\name{generate_schedule}
\alias{generate_schedule}
\title{Generate a provider schedule matrix}
\usage{
generate_schedule(
  n_providers,
  n_days,
  schedule_type = DEFAULT_SCHEDULE_TYPE,
  seed = 42,
  weights = NULL
)
}
\arguments{
\item{n_providers}{Integer number of providers (capped at 5000).}

\item{n_days}{Integer window length in days.}

\item{schedule_type}{One of \code{"3/7 Day"}, \code{"3/7 Night"}, \code{"4/7 Day"},
\code{"4/7 Night"}, \code{"Progressive (day & night mix)"}, \code{"Random"}. The \code{"Random"}
type draws each day from the paper's empirical weights
(d = 0.246, n = 0.230, o = 0.524); pass \code{weights} to override, e.g.
\code{c(d = 1/3, n = 1/3, o = 1/3)} for an equal split.}

\item{seed}{Integer RNG seed.}

\item{weights}{Optional named numeric with elements \code{d}, \code{n}, \code{o}. When
supplied, every provider-day is drawn from these probabilities and
\code{schedule_type} is ignored.}
}
\value{
A character (providers x days) matrix of \code{"d"}/\code{"n"}/\code{"o"}.
}
\description{
Each provider receives an independently randomised schedule satisfying the
chosen type. Seeding is applied once at the population level so results are
reproducible for a given set of inputs.
}
\examples{
sched <- generate_schedule(10, 28, "3/7 Day", seed = 1)
table(sched)
}
