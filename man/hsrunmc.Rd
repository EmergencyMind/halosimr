% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montecarlo.R
\name{hsrunmc}
\alias{hsrunmc}
\title{Run a Monte Carlo ensemble of HALO simulations}
\usage{
hsrunmc(
  n,
  schedule_fn,
  events_fn,
  training_program = "none",
  providers = NULL,
  probs = c(0.1, 0.5, 0.9),
  seeds = NULL,
  ...
)
}
\arguments{
\item{n}{Integer number of runs.}

\item{schedule_fn}{Function of one argument (a seed) returning a schedule
matrix, e.g. \code{function(s) generate_schedule(200, 365, "3/7 Day", seed = s)}.}

\item{events_fn}{Function of one argument (a seed) returning an events data
frame, e.g. \code{function(s) generate_events(365, rate = 0.1, seed = s)}.}

\item{training_program}{Program for the trained arm. \code{"none"} runs the
baseline only (no lift).}

\item{providers}{Optional provider ids passed to \code{\link[=halo_sim]{halo_sim()}}.}

\item{probs}{Length-3 numeric of lower/median/upper quantiles for the band.}

\item{seeds}{Optional integer vector of length \code{n}; defaults to \code{1:n}.}

\item{...}{Further arguments passed to \code{\link[=halo_sim]{halo_sim()}} (readiness model and
parameters, \code{training_interval_days}, etc.).}
}
\value{
An object of class \code{halo_mc} with the per-run readiness matrices, the
baseline band (and trained band + \code{lift} when a program is set), stored
per-run \code{gap_max}, and a per-run \code{summary} data frame.
}
\description{
Run a Monte Carlo ensemble of HALO simulations
}
\examples{
mc <- hsrunmc(
  n = 10,
  schedule_fn = function(s) generate_schedule(100, 180, "3/7 Day", seed = s),
  events_fn   = function(s) generate_events(180, rate = 0.15, seed = s),
  readiness_model = "exponential", training_program = "monthly"
)
mc
}
